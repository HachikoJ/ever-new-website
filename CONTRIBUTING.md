# Contributing

欢迎通过 Issue 提交官网内容、入口或部署问题。提交前请在 Node.js 20+ 环境运行：

```bash
npm install
npm run lint
npm run build
```

Pull Request 请说明变更目的、验证命令和是否影响产品域名跳转。请不要提交密钥、用户数据、`node_modules/`、`.next/` 或 `out/`。
